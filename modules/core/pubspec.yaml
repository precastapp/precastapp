name: core
description: A core modulor for base other modules.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.0.2

# Add regular dependencies here.
dependencies:
  shared_preferences:

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
